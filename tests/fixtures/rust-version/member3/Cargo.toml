[package]
name = "member3"
version = "0.0.0"
publish = true
rust-version = "1.64"

[features]
default = ["a"]
a = []
b = []
c = []

[dependencies]
member2 = { path = "../member2" }

[dev-dependencies]
